package com.example.critichub.data.local.prefs

/**
 * حالت تم برنامه. [SYSTEM] حالت پیش‌فرض (پیروی از تم سیستم) است و در تنظیمات
 * فقط دو گزینهٔ LIGHT و DARK به کاربر نمایش داده می‌شود.
 */
enum class ThemeMode {
    SYSTEM,
    LIGHT,
    DARK
}
